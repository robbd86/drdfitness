# Example server environment variables
# Copy to `server/.env` (or your deployment provider's env vars) and fill in real values.
# Do NOT commit real secrets.

# -------------------------------
# App runtime
# -------------------------------
# Set to "production" in deployed environments.
NODE_ENV=development

# Port for the Express server (Railway sets this automatically in production)
PORT=5001

# -------------------------------
# Database
# -------------------------------
# Primary Postgres connection string (required). Used by Drizzle and also for session storage.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/workout_builder

# Optional fallback connection string if you use Neon.
NEON_DATABASE_URL=postgresql://neondb_owner:example@ep-example.neon.tech/neondb?sslmode=require

# -------------------------------
# Sessions & authentication
# -------------------------------
# Secret used to sign/verify Express session cookies (required).
# Use a long random string (recommended: 32+ bytes).
SESSION_SECRET=change-me-use-a-long-random-string

# Secret used to sign password-reset JWTs (recommended).
# The password reset routes will use this first, and fall back to SESSION_SECRET if unset.
# Format recommendation: 64 hex chars (32 bytes), e.g. output of `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
PASSWORD_RESET_JWT_SECRET=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Optional: in-memory used-token tracking (NOT suitable for multi-instance deployments).
PASSWORD_RESET_TRACK_TOKENS=false

# -------------------------------
# Frontend URL
# -------------------------------
# Public base URL for the frontend app, used to build password reset links.
CLIENT_URL=http://localhost:5173

# -------------------------------
# Email (Resend)
# -------------------------------
# Resend API key (required in production for emails).
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# From address used by the password reset email sender (required in production).
EMAIL_FROM="DRD Fitness <no-reply@yourdomain.com>"

# From/admin addresses used by server/utils/email.ts (welcome/admin emails).
FROM_EMAIL="DRD Fitness <no-reply@yourdomain.com>"
ADMIN_EMAIL=admin@yourdomain.com
